<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content=" {{ title }}" />
    <meta name="author" content="geomydas" />
    {% set css %} {% include "styles.css" %} {% endset %}
    <style>
  	{{ css | cssmin | safe }}
    </style> 
    <title>{{ title }}</title>
  </head>
  <body>
    <main>
      <h1>{{ title }}</h1>
      {{ content | safe }}
      <a href="/" id="b">← Go back</a>
    </main>
  </body>
</html>
